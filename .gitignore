# reverie (repo name on GitHub: divergence)

# Source law PDFs, not code — one of them is 171MB, over GitHub's 100MB hard
# limit, so this folder cannot be pushed as-is regardless of intent.
New folder/

# Regenerated by killgate.py — API pulls, not authored content.
/cache/

# cleanup_repo.py moves retired/duplicate files here. Nothing deleted, never committed.
/_archive/

__pycache__/

# See divergence/.gitignore for the keys/secrets rules that matter most —
# git honours nested .gitignore files, so those still apply under divergence/.
.claude/
