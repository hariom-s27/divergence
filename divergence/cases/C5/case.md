# CASE C5 — $3,000 · US client · bank wire · SUNDAY settlement

**Input type:** natural   ·   **Determinate overall:** no

USD 3,000 wired by a US client, value-dated Sunday 28 June 2026. FIRC issued.
NO CRYPTO ANYWHERE. Yet SBI published no rate on the 28th and neither did FBIL, so
'which official date' is open on a plain fiat receipt.

THIS IS THE FALLBACK DEMO. If the crypto leg is ever challenged on stage, this case
makes the same point with no exchange data, no proxy, and no token.

---
## TO DO — P1

1. Produce the actual input document(s) for this case in this folder
   (`invoice.pdf` / `payment.png` / `payment.txt`). Typed input is acceptable
   for at least two cases — say so openly rather than hiding it.
2. Fill `ground_truth.json`. **Before any model runs.**
3. `git add cases/ && git commit` — then quote that hash in results.md.

**If you fill this in after seeing a pipeline output, the entire evaluation
is worthless and the damage is invisible.**
